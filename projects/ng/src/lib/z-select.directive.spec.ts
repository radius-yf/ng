import { ZSelectDirective } from './z-select.directive';

describe('ZSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ZSelectDirective();
    expect(directive).toBeTruthy();
  });
});
